# Recordings: large, private, and reproducible into transcripts.
*.wav
*.mp4
*.mkv
*.m4a
*.flac

# Runtime state
recording.json
stop.flag
app.lock
.watcher-seen.json

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# Models
*.onnx
models/

# Transcripts and notes are deliberately NOT ignored — they are the point.
